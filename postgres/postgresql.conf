# Cấu hình cơ bản
listen_addresses = '*'
max_connections = 100

# Cấu hình WAL
wal_level = logical
max_wal_senders = 4
max_replication_slots = 4

# Các tham số khác
shared_buffers = 128MB
dynamic_shared_memory_type = posix
